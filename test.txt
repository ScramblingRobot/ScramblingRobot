git clone
	**look up new way of doing this (more secure)
	clone to repository into a your local machine

git status
	displays the current status

git add .
	

git commit -m "message"

git push

git pull
	equivalent to git fetch and git merge
	can run into merge conflicts

git branch

git checkout

git log